# SANTRACK central API
PORT=8081

DB_HOST=localhost
DB_PORT=5433
DB_NAME=santrack
DB_USER=stock
DB_PASSWORD=stock_dev

# DEV-ONLY secret - replace with a strong random value (>= 32 bytes) in production
JWT_SECRET=santrack-dev-secret-change-me-0123456789abcdef0123456789abcdef
JWT_EXPIRES_IN=24h

CORS_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5173

# File storage. "local" writes under STORAGE_LOCAL_ROOT.
# Swap for a hosted driver (e.g. cloudinary) without touching licensing code.
STORAGE_DRIVER=local
STORAGE_LOCAL_ROOT=./var/uploads


# Reverse-proxy hops to trust when deriving the client IP for rate limiting.
# 0 for a directly-exposed process; set to the number of proxies in front of
# the app (usually 1 behind a single load balancer). Trusting more hops than
# actually exist lets a client forge X-Forwarded-For and evade the limits.
TRUST_PROXY_HOPS=0

# Per-address ceilings on the unauthenticated routes. Set a limit to 0 to
# disable that policy (useful for the walkthrough and local development).
RATE_LIMIT_LOGIN=20
RATE_LIMIT_LOGIN_WINDOW_MS=900000
RATE_LIMIT_REGISTER=20
RATE_LIMIT_REGISTER_WINDOW_MS=3600000
# Loose by design: consumers scanning products share carrier NAT addresses.
RATE_LIMIT_VERIFY=300
RATE_LIMIT_VERIFY_WINDOW_MS=60000

# Licensing. "advisory" (default) records a compliance finding and notifies the
# holder but lets the operation through - the supervisory model the proposal
# describes. "strict" refuses instead. "off" disables the check. Revoked
# licences are refused in every mode.
LICENSING_ENFORCEMENT=advisory
LICENSING_PROVISIONAL_DAYS=90

# How far ahead the nightly sweep warns holders about expiring stock.
NEAR_EXPIRY_DAYS=30
